let count = 0;
let fselect = "";
let sselect = "";

const cards = document.querySelectorAll(".cards, .card");
cards.forEach ((card) => {
    card.addEventListener ("click", () => {
        console.log (true)
    });
});